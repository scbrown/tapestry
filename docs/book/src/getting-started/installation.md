# Installation

Tapestry is a single Go binary. Build from source:

```bash
go build -o tapestry ./cmd/tapestry
```

Place the binary on the target host and configure via `~/.config/tapestry/config.toml`.
